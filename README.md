# YOLO26s-seg 智能分割训练平台

这是一个基于 **FastAPI (后端)** 和 **Vue 3 (前端)** 构建的轻量化 YOLO26s-seg 实例分割训练控制台。专为猪只分割数据集定制，支持自动化数据集解压、随机划分、YOLO 格式 data.yaml 生成、数据增强调整以及控制台日志实时监控。

> 临时放行 9523，如果系统用了 firewalld：
先查看状态：

```bash
sudo firewall-cmd --state
```

放行端口：

```bash
sudo firewall-cmd --permanent --add-port=9523/tcp
sudo firewall-cmd --reload
```
---

## 🌟 核心功能

1. **一键数据集准备**：自动解压并按照指定比例随机划分 `train` / `val` / `test` 数据集。
2. **负样本智能管理与一键保存**：提供专门的“保存为负样本”按钮，可将图片一键重命名为递增编号 `负样本{N}` 并创建空 `.txt` 文件参与训练。同时优化分类筛选逻辑，支持“已标”、“未标”、“负样本”状态过滤。
3. **训练参数自定义**：配置 epochs、batch size、学习率 (lr0)、早停耐心值 (patience)、输入图片尺寸 (imgsz) 和设备 (device)。
4. **数据增强配置**：直观控制 Mosaic、MixUp、Copy-Paste、旋转及水平/垂直翻转概率。
5. **实时训练监控**：基于 SSE 技术的极客终端，实时流式显示 YOLO 训练控制台输出及当前 Epoch 进度。
6. **硬件负载监测**：实时刷新服务器的 CPU、内存占用以及 GPU 加速检测状态。
7. **一站式多模式标注**：
   - **手动绘制**：高精度点击打点，支持双击/点击首点闭合多边形。
   - **边线点击直接加点并拖拽**：移除了干扰视线的半透明虚拟中点，改为直接点击多边形各边线上的 6px 隐形热区即可在点击坐标瞬间创建顶点，无需松开鼠标即可任意拖动微调；双击顶点快速删除。
   - **一键模型自动识别**：加载已有 YOLO-seg 模型直接预测猪只分割，并调用 RDP 多边形拟合简化算法，减少顶点点数便于人工二次调整。
   - **SAM 智能辅助标注**：左键打绿点（正点），右键打红点（负点），基于 Segment Anything 2.0 模型实时产生高契合度轮廓，按 Enter 键或点击「保持」按钮直接确认生成标注。
   - **涂抹擦除（橡皮擦）**：支持独立的橡皮擦工具，或在编辑模式下按住 `Alt` 键拖动鼠标，即可在画布中连续涂抹擦除一定半径内的顶点（支持 `[` / `]` 快捷键调节半径，并伴有半透明红色范围指示圈，多边形擦除至少于 3 个点时会自动清理）。
8. **浅色与深色模式自适应**：系统默认以暗黑模式启动，右上角可一键切换，并在浏览器缓存中记忆选择。
9. **训练历史与最佳权重下载**：当系统处于空闲（非训练）状态时，自动检测并展示最近一次训练的详细指标数据（Box Loss, Segmentation Loss, Class Loss, mAP50, mAP50-95）、已训练完成的轮次、以及训练所采用的具体数据集名称。支持一键下载保存在 `weights` 目录下的最佳分割模型 `best.pt`，并直接渲染训练收敛评估曲线图 `results.png`。


---

## 📂 项目结构

```
xnl-training-platform/
├── datasets/                    # 数据集放置目录
│   ├── labeling/                # 本地多数据集管理根目录
│   │   └── {dataset_name}/      # 每一个子文件夹都是一个独立的数据集
│   │       ├── images/          # 用户上传的当前数据集待标图片目录
│   │       ├── labels/          # 当前数据集自动保存的归一化 YOLO-seg labels 文件 (.txt)
│   │       └── classes.txt      # 当前数据集的分类标签清单
│   └── 赶猪通道图集_yolo.zip    # 已标注的数据集压缩包 (未配置本地数据集时回退数据源)
├── models/                      # 预训练模型放置目录
│   └── yolo26s-seg.pt           # 预训练权重
│   # 如果要使用 SAM 辅助，建议将权重下载并命名为 models/sam2_b.pt
├── server/                      # FastAPI 后端服务
│   ├── main.py                  # API 路由、标注算法与系统监控
│   ├── trainer.py               # 训练状态机与子进程管道解析
│   ├── dataset_utils.py         # 本地标注优先 / 压缩包解压 8:1:1 自动划分逻辑
│   └── requirements.txt         # 依赖清单
└── web/                         # Vue 3 前端项目
    ├── src/
    │   ├── App.vue              # 融合配置大屏与交互式标注平台的主单页
    │   └── style.css            # 双主题自适应科技感样式系统
    └── package.json
```

---

## 📊 数据集与负样本规则

* **标注数据集优先训练法则**：如果 `datasets/labeling/images` 目录下存在任何已上传图片，系统在点击“一键准备数据集并开始训练”时，将**优先直接划分本地标注目录中的图片和标签**，彻底免去每次解压 zip 压缩包的流程。
* **负样本保存与自动重命名**：对于不需要标注的背景负样本，可在数据标注平台直接点击「保存为负样本」按钮。系统会自动将其重命名为 `负样本{自动递增编号}` 并同时在 `labels` 目录下创建同名的空 `.txt` 文件。这能够防止由于未标数据无序分布干扰标注过程，并在训练时提供正规的 YOLO 格式背景负样本。
* **清空标注重置为未标**：如果在页面上清空了多边形并点击「保存标注」，系统会物理删除其对应的 `.txt` 文件，使其恢复为「未标」状态，防止产生空标签文件导致被误认为是有意的负样本。

---

## 🚀 部署与运行指南

本平台支持 **开发联调双服务模式** 以及 **生产单端口一体化托管模式**。

### 方式一：生产环境单端口一体化托管（推荐 🌟）

此方案**不需要**在 GPU 服务器上启动 Node 服务或配置 Nginx，直接使用 FastAPI 后端托管编译后的前端静态文件。

1. **在开发机或服务器上打包前端**：
   ```bash
   cd web
   pnpm install
   pnpm build
   ```
   打包完成后会在 `web/dist` 目录下生成静态文件。
2. **在服务器上启动后端服务**：
   ```bash
   cd server
   pip install -r requirements.txt
   python main.py
   ```
   **效果**：运行 `python main.py` 后，FastAPI 会自动检测并挂载 `web/dist` 目录。您只需在浏览器中直接访问 `http://服务器IP:9523/` 即可同时使用完整的 Web 控制台和后端服务。这种方式从根本上避免了跨域 (CORS) 问题，且部署极为省心。

---

### 方式二：双开发服务联调模式

如果您需要修改前端代码并实时热更新，可选择本模式：

1. **启动后端 API 服务**：
   ```bash
   cd server
   python main.py
   ```
2. **启动前端 Vite 服务**：
   ```bash
   cd web
   pnpm dev
   ```
   启动后，浏览器访问 `http://localhost:5173` 即可。

---

### 方式三：使用 Nginx 代理部署（进阶方案）

如果您的服务器使用 Nginx 统一管理多个站点，可以参考以下配置：

1. **打包前端**：
   在 `web` 目录下运行 `pnpm build`，将生成的 `web/dist` 文件夹拷贝到服务器的 `/var/www/yolo-platform` 目录。
2. **Nginx 配置示例**：
   ```nginx
   server {
       listen 80;
       server_name your_server_ip_or_domain;

       # 前端静态资源托管
       location / {
           root /var/www/yolo-platform;
           index index.html;
           try_files $uri $uri/ /index.html;
       }

       # 后端 API 接口转发
       location /api {
           proxy_pass http://127.0.0.1:9523;
           proxy_http_version 1.1;
           proxy_set_header Upgrade $http_upgrade;
           proxy_set_header Connection "upgrade";
           proxy_set_header Host $host;
           
           # SSE 流式日志所需的缓冲关闭配置
           proxy_buffering off;
           proxy_cache off;
       }
   }
   ```
3. **启动 Python API 后台进程**（使用 `nohup` 或 `pm2` 等守护进程）：
   ```bash
   cd server
   nohup python main.py > server.log 2>&1 &
   ```

---

## 💻 另一台 GPU 服务器部署说明

由于本平台在开发阶段采用 CPU 运行，部署至 GPU 服务器时需要执行以下配置：

1. **显卡驱动与 PyTorch GPU 安装**：
   确保 GPU 服务器已安装 NVIDIA 驱动，并安装 CUDA 版本的 PyTorch。例如，若显卡支持 CUDA 12.1：
   ```bash
   pip install torch torchvision --index-url https://download.pytorch.org/whl/cu121
   ```
2. **在 Web 中配置训练设备**：
   在 Web 界面左侧的“训练设备”输入框中，将 `cpu` 改为 GPU 的序号：
   * 单显卡配置：`0`
   * 多显卡并行（需 YOLO 分布式训练）：`0,1`
3. **系统自动识别**：
   后端会自动检测 PyTorch 显卡加速状态，若 GPU 加速就绪，系统负载仪表盘中的 "GPU 加速" 将显示为绿色的 "已启用"，并展示对应的显卡型号。

---

## 📝 更新日志

### 2026-07-01
* **新增**：支持多边形顶点的“涂抹擦除（橡皮擦）”功能。
  * 提供独立的橡皮擦工具，可在工具栏中选择，并通过滑块或 `[` 与 `]` 键盘快捷键方便地调节擦除半径。
  * 支持在编辑模式下，按住 `Alt` 键的同时拖动鼠标左键直接进入快速涂抹擦除状态，松开 `Alt` 后立刻恢复，极大地提升了大批量多余顶点清理和微调的效率。
  * 在画布中渲染半透明红色的虚线跟随指示器以表明擦除范围，并实现少于3个点时自动清理无效几何多边形的保护机制。
* **优化**：修复保存标注或保存负样本后，按空格键会误触发提交按钮的体验问题。
  * **原理**：保存成功并切换下一张图后，焦点仍停留在“保存标注”或“保存为负样本”按钮上，此时按下空格键（Space）会激活当前 Focused 的按钮触发二次提交。
  * **解决方案**：在保存标注及保存负样本的动作开头主动调用 `document.activeElement.blur()` 使按钮失焦；并在全局 `handleKeyDown` 中判断当焦点不在文本输入框（INPUT/TEXTAREA）或富文本编辑框时，阻止空格键的默认 click 行为，双重规避误提交问题，保障图片平移缩放体验。

### 2026-06-24
* **优化**：修复“预计剩余时间 (ETA)”估算不准且在某些状态下一直是 `0` 的 Bug。
  * 改进了 `tqdm` 进度条的正则提取逻辑，采用精准前缀匹配，防止与控制台中的其他数值关系（如学习率、Loss `<0.001` 等）产生误匹配导致 ETA 归零。
  * 调整了解析流的控制逻辑，将进度条时间解析前置并剥离 `return`，确保同一行日志中同时存在 Epoch 数据与进度条数据时，ETA 指标不会因前面的匹配提前 `return` 而被跳过。

### 2026-06-26
* **新增**：标注模式模型一键识别悬停下拉列表 (Hover Dropdown) 功能。
  * 支持自动扫描并罗列出系统 `models/` 目录与已训练 `runs/` 目录下的所有 pt 权重模型，方便用户自主选择特定的历史权重或默认权重进行自动识别。
* **重构**：引入高颜值自研 Toast 轻提示系统，全面淘汰了阻塞浏览器主线程的 `alert` 提示，提升交互的丝滑度与顺畅感。
* **优化**：标注工作台图集列表与工具布局大升级。
  * **快速过滤 Tab**：在左侧列表增加“全部”、“已标注”、“未标注”状态过滤，轻松承载千张以上的大数据集并提高加载效能。
  * **列表轻量化与瘦身**：移除冗余的文件大小 K 字节显示，引入数字序号索引与“已标/未标”文字 Badge 直观区分状态；**物理删除了过小且重复的 `status-dot` 小圆点**，并将图片文件名最大宽度放大至 135px，显示更齐全。
  * **工具栏合并**：将“一键模型识别”按钮收纳至左侧工具栏中（同“SAM辅助”、“手动打点”并列），并将下拉菜单背景修改为**完全不透明**（亮色纯白/暗色纯深灰 `#12121e`），采用左对齐定位，防止被背景图片干扰且避免溢出屏幕边界。
  * **手动绘制优化**：修复未闭合多边形折线默认被 SVG 填充为黑色的遮挡 Bug（改为 `fill: none`），确保打点过程中底层画面清晰可见。
  * **控制点与线宽极致微缩，移除中点干扰**：
    - **顶点微缩**：编辑顶点半径降至 `1.8px`，打点提示点降至首点 `2.5px`/其他 `1.5px`，SAM 提示点降至 `2.5px`；多边形线宽整体减半（未激活 `0.75px`，激活 `1px`）。
    - **无感交互**：去除了悬浮与拖拽顶点时的变大和白边效果，保持原 1.8px 大小不动以防微调时遮挡视线，且将顶点鼠标样式恢复为指针样式（`pointer`）。
    - **移除中点，改为边线直接加点**：彻底废弃了原先相邻顶点之间的“半透明虚拟中点”渲染；重构为在多边形各边线上加入 6px 的隐形热区 hitbox (`.svg-edge-hitbox`)，鼠标悬浮时仅显示淡淡的紫色导线，直接点击该边线的任意像素位置即可生成新顶点，并可立刻直接拖拽微调。
  * **缩放抗锯齿与清晰度重构**：移除了缩放容器的 `will-change: transform` 强缓存标记，迫使浏览器在缩放画布时对矢量图层进行重新光栅化；同时对所有 SVG 元素强制启用 `vector-effect: non-scaling-stroke`，使得线条和点在放大数倍后依然保持 1px~1.5px 的纤细物理线宽，不再产生拉伸模糊或粗重锯齿。
  * **SAM 负点（右键红点）约束机制修正**：
    * 修复了由于 SAM 模型对于多点微调要求三维嵌套数组格式（即 `points=[[[x1, y1], [x2, y2]]]` 和 `labels=[[1, 0]]`），而先前被后端传成了多个独立目标的二维格式导致右键红点对前景毫无修正作用的问题。
    * 修改前端 `startPan` 事件，排除 SAM 模式下的鼠标右键操作，彻底消除了右键点击添加背景红点时，红绿提示点频繁闪烁和画面漂移的交互冲突。
    * 对 SAM 正/负提示点（红绿圆点）的 CSS 动画进行去闪烁优化：通过设置 `pointer-events: none` 使鼠标穿透这些临时点，从而彻底消除了鼠标滑过提示点时由 CSS `transform: scale` 偏移引起的反复进入/移出 hover 状态带来的“高频闪烁”现象，且打点时不会因为点重叠而遮挡鼠标点击落点。
  * **精简顶部 Header 高度**：将全局 container 外边距整体收缩 50%（padding: 12px 16px, gap: 12px），标题字号与 LOGO 图标整体缩减至 20px，重新编排 subtitle 贴合度。为下方的数据画布与监控控制台额外腾出了可观的垂直高度。
  * **默认启动暗黑模式**：重写前端主题控制状态，将平台启动默认皮肤调整为**暗黑模式（Dark Theme）**。在用户没有主动清除或存在缓存倾向时，一律以高科技感的极客深色磨砂大屏状态启动。
  * **多目标防误触与眼睛显隐隔离**：在右侧实例列表中新增了“眼睛”显隐切换图标。用户在编辑密集多边形时，可以一键将非活动的目标实例在 SVG 画布上隐藏（不渲染，完全杜绝鼠标事件捕获），彻底防止了误触相邻顶点的问题；同时将分类选择 Select 宽度由 100px 压缩至 70px。
  * **移动平移与打点防误触冲突阻断**：在 `handleSVGClick` 和 `handleSVGMouseDown` 触发时加入了平移状态安全锁。当用户正在按住空格键拖拽画面、切换为手形工具、或者刚刚松开鼠标结束平移时，平台会自动阻断点击，绝对不会在松开鼠标时误触生成多边形顶点或 SAM 正负点。
  * **图集列表删除按钮悬浮叠加与进度显示**：将左栏图集项 `.file-item` 设为相对定位，删除按钮改为绝对定位且悬浮叠加（仅在 Hover 时显现并拥有高亮反馈），释放了排版宽度，消除了右侧留白的视觉硬伤；同时在左栏主标题中加入了全局标注进度显示（已标图片数量/总图片数量），并在列表图片项的“已标”状态标签中同步显示已标的实例数量，如“已标 X 个”（点击保存标注后实时计算刷新）。
* **适配**：补充并在 `server/requirements.txt` 中写入 `python-multipart`，进一步规避了前端 Form-data 上传依赖丢失的部署隐患；前端项目通过 Vite 重新编译发布并完成了生产包构建。

### 2026-06-30
* **新增**：多数据集选择与创建功能。
  * 在顶部 Tab 导航栏右侧增加了精致的数据集下拉选择框。
  * 标注页面状态下支持“新建数据集”按钮，点击即可创建对应的目录并自动切换。
  * 自动兼容性迁移：后端启动时会自动检测并迁移旧有的单数据集结构至 `/datasets/labeling/default`，保障向后兼容。
* **新增**：级联删除分类标签功能。
  * 标注平台支持在右侧一键删除不再需要的分类标签（要求保留至少一个类别）。
  * 实现了后端级联标签清洗算法：删除类别时自动清洗该数据集下的所有 labels 文件，剔除对应标注并智能递减更新高于被删类别的 class_id 索引。
* **优化**：新增“保存标注”及“保存为负样本”后的自动跳转下一张未标图片的交互机制。
  * **自动跳转**：在保存标注或保存负样本成功后，标注区域会自动计算并切换加载当前过滤列表下的下一张图片，避免保存后画布滞留在原已消失图片上的不一致体验。
  * **状态同步**：保存标注时，前端会实时更新图片的 status 状态，使其根据当前的过滤条件实时隐藏并平滑过渡到下一张。

### 2026-06-29
* **新增**：在开始模型训练前自动检测未标注图片并提供二次确认提示。
  * 点击“开始训练”时，前端会自动查询待标图片列表，统计并展示未标注的图片数量。
  * 若检测到存在未标注的图片，会弹出提示（“尚有 xx 张图片未标注，未标注的图片将作为背景训练。是否继续？”），确认后方可拉起训练，防止未标注的数据被无意作为背景负样本带入训练。
* **新增（负样本功能）**：
  * 在保存标注按钮旁新增「保存为负样本」功能，自动重命名图片为递增的 `负样本{N}` 并生成空 `.txt` 标签文件。
  * 优化待标图集过滤，移除“全部”，修改为「已标」、「未标」、「负样本」三状态筛选，且默认选中「未标」。
  * 优化「保存标注」逻辑，在清空标注并保存时自动物理删除其标签文件，使其回归「未标」状态，避免误认为负样本。
